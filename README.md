# sem 2 k 2

Приложение ASP.NET Core с базой данных SQLite. Для запуска в Docker выполните:

```bash
docker compose up --build
```

После сборки сайт будет доступен по адресу [http://localhost:8080](http://localhost:8080).
